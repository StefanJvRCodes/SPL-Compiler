glob { x } 
proc { } 
func { 
    add ( a b ) { 
        local { result } 
        result = ( a plus b ) ; 
        return result 
    } 
} 
main { 
    var { total } 
    total = add ( x 5 ) ;
    halt 
}
